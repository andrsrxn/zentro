{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",

  "globalDependencies": [".env", "tsdown.config.ts", "tsconfig.json"],

  "globalEnv": [
    "NODE_ENV",
    "DATABASE_URL",
    "PORT",
    "BASE_URL",
    "WEB_URL",
    "NEXT_PUBLIC_BASE_URL",
    "NEXT_PUBLIC_API_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^typecheck", "^lint", "^build", "typecheck", "lint"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "tsconfig.json", "tsdown.config.ts"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "build:types": {
      "dependsOn": ["^build:types"],
      "inputs": ["tsconfig.json"],
      "outputs": ["dist-types/**"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },

    "typecheck": {
      "dependsOn": ["^build:types"],
      "outputs": []
    },
    "start": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build:types"],
      "outputs": ["coverage/**"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "db:generate": {
      "cache": false
    },
    "db:migrate": {
      "cache": false
    },
    "db:push": {
      "cache": false
    },
    "db:studio": {
      "cache": false
    },
    "db:auth:docs": {
      "cache": false
    }
  }
}
